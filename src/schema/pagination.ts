/**
 * @module schema/pagination
 * @description Pagination schemas and utilities for list operations
 *
 * @example
 * ```ts
 * import { PaginationParams, PaginatedResponse, paginate } from "@tesserakit/core/schema";
 *
 * // Define paginated response for your entity
 * const PaginatedPersons = PaginatedResponse(PersonSchema);
 * ```
 */

export * from "./pagination";
